{
  "name": "apigestiondocumental",
  "version": "1.0.0",
  "description": "api del proyecto de gestion documental para la fesc",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=.env && node src/server.js",
    "dev": "set NODE_ENV=.env.dev && nodemon src/server.js"
  },
  "author": "Daniel Velandia",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.1",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
